overwrite: true
schema: 'https://api-dev.mavryk.finance/v1/graphql'
generates:
  src/utils/generated/graphqlTypes.tsx:
    plugins:
      - 'typescript'
config:
  scalars:
    bigint: number
    float8: number
    smallint: number
    jsonb: unknown
    timestamptz: string
