// Default config points to user app — keeps `yarn start` and `yarn build` working.
// For explicit app builds, use vite.user.config.mts or vite.gov.config.mts.
export { default } from './vite.user.config.mts'
